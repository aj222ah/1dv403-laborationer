"use strict";
var ADAJAWM = ADAJAWM || {};
ADAJAWM.rss = ADAJAWM.rss || {};

ADAJAWM.rss.RSSManager = {
    init: function() {
        alert("Funkar!");
    },
};

window.addEventListener("load", ADAJAWM.rss.RSSManager.init, false);